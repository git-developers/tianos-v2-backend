{% extends 'base.html.twig' %}

{% block title %}Gato3{% endblock %}

{% block body %}
    <h1>Gato3</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gato3.id }}</td>
            </tr>
            <tr>
                <th>Aaaaa</th>
                <td>{{ gato3.aaaaa }}</td>
            </tr>
            <tr>
                <th>Bbbbbb</th>
                <td>{{ gato3.bbbbbb }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gato3_index') }}">back to list</a>

    <a href="{{ path('gato3_edit', {'id': gato3.id}) }}">edit</a>

    {{ include('gato3/_delete_form.html.twig') }}
{% endblock %}
